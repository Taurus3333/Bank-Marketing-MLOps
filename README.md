# Bank Marketing — Lean MLOps

Chunk 1: minimal skeleton with `src/config.py` (UPPERCASE constants), `src/logger.py`, `src/exceptions.py`, pre-commit, pytest smoke.  
Sandbox by default via `.env`. Next: PySpark ETL → Parquet.
